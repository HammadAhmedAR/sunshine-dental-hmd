# Database configuration loaded from the classpath (src/main/resources/db.properties).
# Real db.properties files are ignored by Git to protect local passwords.
db.url=jdbc:postgresql://localhost:5432/sunrise_dental_clinic
db.username=postgres
db.password=postgres

